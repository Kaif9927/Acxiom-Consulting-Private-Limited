DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=event
SESSION_SECRET=change-me

# Optional: browser origins allowed to call this API with cookies (comma-separated, no spaces unless quoted).
# Example: ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# ALLOWED_ORIGINS=

